<Window x:Class="DeskoptStyle.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeskoptStyle.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBox Name="txtbox" FontSize="42" Margin="5"></TextBox>
            <Button Command="{Binding ShowCommand}" Style="{StaticResource btnstyle}">SHOW</Button>
            <ComboBox Name="o">
                <ComboBoxItem Content="orxan"></ComboBoxItem>
                <ComboBoxItem Content="Nazim"></ComboBoxItem>
                <ComboBoxItem Content="Qabil"></ComboBoxItem>
            </ComboBox>
            <Button Command="{Binding ShowCommandbt}" Style="{StaticResource btnstyle}" >Print</Button>
        </StackPanel>

    </Grid>
</Window>
